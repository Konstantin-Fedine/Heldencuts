# HeldenCuts

Klassische statische Website mit HTML und CSS.

Die Website besteht aus:

- `index.html`
- `public/global.css`

Die Seite kann direkt als HTML geöffnet oder mit einem beliebigen statischen Webserver ausgeliefert werden.
